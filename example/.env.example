# Required values. Generate unique values before `docker compose up`.
MYSQL_DATABASE=gateway
MYSQL_TEST_DATABASE=gateway_test
MYSQL_USER=gateway
MYSQL_PASSWORD=
MYSQL_ROOT_PASSWORD=
GATEWAY_ENVIRONMENT=development
GATEWAY_JWT_SECRET=
GATEWAY_ENCRYPTION_KEY=
# Optional host port (defaults to 8000).
GATEWAY_PORT=8000
