server:
  port: 3000
  mode: release  # debug, release, test

database:
  path: ai-gateway.db

auth:
  jwt_secret: ""
  jwt_expiry: 8h

timeout:
  connection: 30s
  response: 180s
  total: 240s
  model_load: 30s

proxy:
  http_proxy: ""
  https_proxy: ""
  no_proxy: ""